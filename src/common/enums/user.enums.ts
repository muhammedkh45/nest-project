export enum RoleType {
  admin = 'admin',
  user = 'user',
  superAdmin = 'superAdmin',
}
export enum GenderType {
  male = 'male',
  female = 'female',
}
export enum ProviderType {
  local = 'local',
  google = 'google',
}

